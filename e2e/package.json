{
  "name": "rembrant-e2e",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "copy": "mkdir -p server/static && cp ./bundle/index.js ./server/static && cp ../src/Main.re ./tests/",
    "clean": "rm -rf bundle && rm -rf src/*.bs.js",
    "test": "jest",
    "compile": "bsb -make-world",
    "build": "fpack ./src/App.bs.js",
    "ci": "npm run clean && npm run compile && npm run build && npm run copy && npm test"
  },
  "jest": {
    "verbose": true,
    "testURL": "http://localhost"
  },
  "author": "Przemyslaw Jan Pietrzak",
  "license": "MIT",
  "dependencies": {
    "jest": "^23.4.2",
    "puppeteer": "^1.6.0"
  },
  "devDependencies": {
    "bs-platform": "^4.0.3",
    "fpack": "^0.5.3"
  }
}
