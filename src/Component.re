/* open Common;

module Com = {
  let createElement = (~name, ~children, ()) => <div>{ text(name) }</div>;
}

let pomo = {
  let createElement = (~name, ~children, ()) => <div>{ text(name) }</div>;
}

type tesla = {
  .
};

let obj: tesla = {
  pub createElement = (~name, ~children, ()) => <div>{ text(name) }</div>
};

let n = <div>
  <Obj name="name" />
</div>; */